import React, { Component } from 'react';
import FieldSet from 'Components/FieldSet';

class Donations extends Component {

  //
  // Render

  render() {
    return (
      <FieldSet legend="Donations">
        <div>
          {/* Donations section - to be configured later */}
        </div>
      </FieldSet>
    );
  }
}

Donations.propTypes = {

};

export default Donations;
